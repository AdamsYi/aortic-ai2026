# GitHub Actions Secrets

- `VPS_HOST`: Oracle VPS public host or IP for SSH deployment.
- `VPS_USER`: SSH login user on the VPS.
- `VPS_SSH_KEY`: Private SSH key that can access the VPS user account.
